# Chrome Storage
HashMap of `tabId-${number}` and `settings`. Examples:
    * `tabId-936794675`: 'on' | 'off'
    * `tabId-936794680`
    * `settings`: {
        fullscreen: boolean;
        autoOn: boolean;
    }
